# Orchor 改版报告

改动已提交在这份仓库副本里(基于 `6a22bb5`,新提交 `940f247`),56 个文件,**+2029 / −3189 行**——删的比加的多,这正是目的。

---

## ⚠️ 先说没做完的事

**我没能在沙盒里跑通 `npm install`,所以 `npm run build` 和 `npx hardhat test` 都没有实际执行过。**依赖安装反复卡在 link 阶段(等了十几分钟仍未完成),这是沙盒网络的限制,不是代码问题。

代码我逐个文件做了静态检查(引用完整性、props 匹配、导入清理、ABI 闭合),但**静态检查不等于编译通过**。请你在本地跑一遍:

```bash
cd Orchor-redesign
npm install
npx hardhat compile        # 合约
npx hardhat test           # 12 个测试,重点看「转让后权限消失」
npm run build              # 前端
```

有报错发给我,我来修。我不想把"应该能跑"说成"跑通了"。

---

## 一、视觉

**设计系统换成黑金凭证。**整站只有三个色族:近黑 `#0a0906`、香槟金 `#c6a96c`、象牙白 `#ede7d8`。原来的棕色卡带调色板、霓虹紫 `premium.css`、五个稀有度色相全部删除。

关键做法是**保留旧类名、只换实现**——`.glass`(71 处)、`.btn-neon`(21 处)、`.input`(50 处)全站组件一行没改就完成换肤。其中 `.text-gradient` / `.gradient-text`(18 处)从彩色渐变改成实心象牙白,一条规则消掉 18 处视觉噪点。

**金是稀缺资源。**一屏不超过三次:黑金卡的边框、黑金卡的名字、当前筛选项的下划线。**主按钮用象牙白,只有黑金卡配金**——奢侈品的规律是最贵的那个才享有特殊材质。

**稀有度改成金属:**青铜 → 白银 → 黄金 → 铂金 → 黑金。同一个金属家族的明度变化,不再是五个打架的色相;而且贵贱直觉不需要学习,不像"紫色=Epic"是游戏黑话。

**背景是极淡的雕版星图。**原型是纽约中央车站的金色星图穹顶——老金融建筑早就示范过怎么把星空的浪漫翻译成雕版语言。删掉了扫描线、纸纹、三个径向光晕、漂浮尘粒。

**卡面从插画换成链上调用曲线。**这一条下面单独说。

---

## 二、诚实性(我认为这次最重要的改动)

删掉的假数据:

- 首页 `154,740 Total Runs` / `Avg Rating 4.7` —— 写死的
- 每张卡的 `SPD 8 / PWR 9 / COST 6` —— 20 张卡全一样
- `marketplace` 用 `Math.random()` 现编挂单价和拍卖倒计时 —— 每次刷新都变,还触发 hydration 报错
- `battle` 用 `Math.random()` 选对手 —— 服务端和客户端选到不同的卡,同样 hydration 报错
- `@@cipherforge` —— 数据带 `@`、组件又加一个

**现在读不到的数据一律显示破折号,永远不显示 0,更不编造。**新导入的技能卡链上调用真的是 0,曲线就是平的,写着"暂无记录"。

理由很直接:这个项目的整个叙事是"一切链上可验证",评委去 Blockscout 一对账就穿帮。**UI 上的假数字是对叙事的反噬,不是装饰。**

---

## 三、卡面为什么是数据

我去看了真实竞品:

| 产品 | 体量 | 卡面 art |
|---|---|---|
| skills.sh(Vercel) | 91,023 个技能 | 零 |
| Agensi | 2,450 个 | 大半没有 |
| Parallel | AAA 手游 | 满配原画,百万级预算 |
| Sorare | 持牌球星卡 | 官方肖像授权 |

这个品类没人靠插画赢,而靠插画赢的那两个你比不起。**唯一竞品拿不出、你天然有的,是链上履历。**所以画框里放调用曲线、铸造进度、创作者收入。

顺带解决:创作者发第 21 张卡自动有"图",零美术成本,双语无碍,任意分辨率锐利,少 3.0MB 加载。

20 张像素画已删除。它们可以留在别处当项目历史,但不进产品——两种美学并存永远比选一种更乱。

---

## 四、合约:ERC-1155

`contracts/OrchorCore1155.sol`(410 行)。旧合约保留未动,方便对照。

**技能卡成为真正的资产:**可转让、钱包可见、能挂订单簿。选 1155 而不是 721,因为同一技能的 N 份互为同质才有单一价格和买卖盘深度——721 只能做成 OpenSea 式一物一价,上不了 §1 的原生 CLOB。

**权限由 `balanceOf` 派生,旧的 `owned` mapping 彻底删除。**这是这类改造最容易出的事故:留着旧 mapping 就会"卖了卡还留着钥匙"。

**`tokenURI` 全链上渲染。**卡面 SVG 由合约当场算出,内联 data URI,不依赖 IPFS 也不依赖你的服务器。网站关了,卡还在钱包里。

### 顺手修掉的三个旧 bug

**1. 分账可被 DoS(真实漏洞)。**旧版 `_splitRevenue` 直接把钱推给创作者。创作者若是会 revert 的合约,**这张卡的解锁和订阅永久失败**——任何人可以注册一张自带 DoS 的技能卡,正常的多签钱包也可能因 gas 不足中招。改成 pull:失败份额进 `pending`,自己 `withdraw()`。测试里用 `RejectPayment.sol` 专门验证了这条。

**2. tokenURI 注入。**技能名会被拼进 JSON 和 SVG,一个叫 `","image":"evil` 的技能可以注入 metadata。`registerSkill` 现在禁掉 `" \ < >` 和控制字符,长度上限 48。**这是全链上 SVG 方案唯一的真实攻击面。**

**3. `topUpEnergy` 不退零钱。**旧版注释写着 "change refunded" 但实际没退,不足 1 Energy 的部分直接进了 treasury。现在真退。

### 二级版税定 7%,不是 70%

一级市场仍是 70/25/5。版税定太高会杀死转手意愿,没有转手就没有价格发现,§1 的订单簿叙事也就落空。可逐卡调,硬上限 10%。注意 ERC-2981 是**信号标准**,市场方自愿遵守。

---

## 五、性能

| 项 | 改动 |
|---|---|
| 图片 | 3.0MB → 0(20 张 640×640 JPG 显示尺寸只有 260×150) |
| 入场动画 | 删除全部 `delay: index * 0.05`(20 张卡要 1 秒才出齐,且卡越多越慢) |
| 字体 | 5 个家族 11 个文件 → 2 个家族,衬线用系统字体 |
| RPC | 20 卡 × 3 读 → `balanceOfBatch` 一次 |
| CSS | 删 `premium.css`(362 行)+ 6 个零引用组件(1393 行) |
| next | 14.2.15 → 14.2.35(前者有官方安全通告) |

另外删掉了 `image-rendering: pixelated` 加三个 CSS filter 的组合——20 张图同屏时每帧逐像素合成,是滚动掉帧的主因。

---

## 六、新增的用户能力:转让

`TransferCardModal.tsx` + 卡组页入口。**这是 1155 改造对用户最直观的差异:卡第一次真正属于他们。**

弹窗里刻意把后果说清楚——转出后调用权立刻失效,因为权限由链上余额决定。

---

## 七、部署注意

**不兼容改动,必须重新部署,不能升级**(旧合约没有 proxy)。

```bash
npm run deploy:orchor:injective
```

部署后重新注册 20 张卡(名字要过字符校验),更新 `NEXT_PUBLIC_ORCHOR_CORE_ADDRESS`,Blockscout 重新验证。

**部署后必做的三条验证:**

1. MetaMask 导入一张卡,确认看得到卡面图像 —— 这是全链上 SVG 是否成功的唯一验收标准
2. A 解锁 → 转给 B → 确认 A 的 `hasAccess` 变 `false`、B 变 `true`
3. 用 `RejectPayment` 那样的合约注册技能并解锁,确认交易成功、钱进了 `pending`

---

## 八、还欠着的

- **链上调用次数没有数据源。**需要事件索引(后端路由或 subgraph)。现在卡面显示"暂无记录"而不是假数字。这是我唯一没有假装完成的功能。
- **API 层仍无鉴权。**第一次读代码时发现的 P0 问题这次没动——`/api/skills/execute` 传谁的 `userId` 就扣谁的 credits,`/api/creator/withdraw` 能把别人的收益提到自己地址。**收真钱之前必须接 SIWE。**
- **链下执行不校验链上权限。**`/api/skills/execute` 不查 `hasAccess`,不解锁也能免费调用。
- `battle` 和 `profile` 页仍用单卡渲染,没接 `SkillGrid`,链上数据显示为破折号。功能正常,只是信息少。
